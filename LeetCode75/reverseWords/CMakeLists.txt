# Specify the minimum version of CMake required
cmake_minimum_required(VERSION 3.10)

# Set the project name
project(reverseWords)

# Add the executable target
add_executable(MyExecutable reverseWords.cpp)